// Make sure when declaring in HTML that the name matches with the one below
// Declares Angular
// [] for other dependencies, if there are other sub apps we want to use
angular.module("IntroAngular", [])
